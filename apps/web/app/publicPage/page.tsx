import { getProfile } from "@/lib/actions";
import React from "react";

const PublicPage = async () => {
  // call the getProfile() function from actions.ts
  return <div>Your Profile Info Here.</div>;
};

export default PublicPage;
